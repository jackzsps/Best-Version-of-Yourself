rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/images/{allPaths=**} {
      // 只有登入且 UID 相符的使用者才能存取自己的照片
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
